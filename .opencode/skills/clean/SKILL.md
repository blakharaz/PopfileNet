---
name: clean
description: Clean and rebuild the .NET solution
---

Clean build artifacts and rebuild the solution.

Commands:
- `dotnet clean` - Clean build artifacts
- `dotnet clean && dotnet build` - Clean and rebuild

Use this skill when the user wants to do a fresh build or resolve build issues.
